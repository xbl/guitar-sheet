---
description: 每实现一个功能需补充对应测试，避免后续改动破坏已有行为
alwaysApply: true
---

# 功能与测试（回归防护）

实现或修改**可行为化**的功能时，同一批改动中应包含**能失败、能复现**的自动化测试，用于锁定当前行为，供以后 `npm test` / `cargo test` 发现回归。

## 必须做

- **纯逻辑**（`src/utils/`、`src/practice/`、chord 解析、composable 里抽出的纯函数等）：在 `src` 下增加或扩展 `*.test.ts`（Vitest），用例直述需求与边界情况。
- **改动 `src-tauri/src/`** 中的行为：在对应模块的 `#[cfg(test)]` 中增加或更新测试，或补充命令级可测逻辑；完成前在 `src-tauri` 下执行 `cargo test` 并绿。
- **提交前**对本次涉及层执行验证：只动前端则 `npm test`；动 Rust 则 `cargo test`；两者都动则都跑。

## 可省略（需说明）

- 纯文案、样式微调、仅文档或配置，且无新分支逻辑。
- 用户明确说「本 PR 不测」或时间盒仅限探测；仍建议在跟进 PR 补测。

## 建议结构

- 新模块旁并列 `foo.test.ts`，或与现有仓库一致（如 `src/practice/scrollGeometry.test.ts`）。
- 复杂 UI 优先把决策逻辑抽到可测函数再测组件；避免只为覆盖而测 DOM 细节。

## 与「全栈功能完成」的关系

客户端与后端都接上仍不够：**没有对应测试则不把该功能视为可安全演进**——后续新功能应靠现有用例先拦住破坏性变更。
